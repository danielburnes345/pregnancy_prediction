Generates data, trains and evaluates the Recurrent Neural Network. Uses a data generation function fetched from a study on pregnancy prediction. Predicts pregnancy based on sexual activity and mood. 
